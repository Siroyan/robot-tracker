[project]
name = "robot-tracker"
version = "1.0.0"
description = "OpenCV tracker for underwater robot thrusters"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.5",
    "opencv-python-headless>=4.13.0.92",
]
